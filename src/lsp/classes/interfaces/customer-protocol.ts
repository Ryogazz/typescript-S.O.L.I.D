export interface custumerOrInterprise {
  getname(): string;
  getIDN(): string;
}

export interface CustomerProtocol {
  firstName: string;
  lastName: string;
  cpf: string;
}

export interface InterpriseProtocol {
  name: string;
  cnpj: string;
}
